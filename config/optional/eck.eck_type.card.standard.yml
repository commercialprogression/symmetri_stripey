langcode: en
status: true
dependencies:
  config:
    - eck.eck_entity_type.card
name: Card
type: standard
description: 'An icon/image, a heading, some text, and a link. All are optional, but each works best in concert with the others.'
